# Copy to .env and fill in from the Lovable project settings.
# Public (safe in the client bundle)
VITE_SUPABASE_URL=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_PROJECT_ID=""

# Server-side aliases (used by /api handlers and serverFns)
SUPABASE_URL=""
SUPABASE_PUBLISHABLE_KEY=""

# Server-only secrets — never expose with a VITE_ prefix
DEEPSEEK_API_KEY=""
# Preferred names (aliases also accepted):
VOLC_ASR_APP_ID=""
VOLC_ASR_ACCESS_TOKEN=""
# Legacy aliases:
VOLC_APP_ID=""
VOLC_ACCESS_TOKEN=""
