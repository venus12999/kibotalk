# Copy to .env and fill in from the Lovable project settings.
# Public (safe in the client bundle)
VITE_SUPABASE_URL=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_PROJECT_ID=""

# Server-only secrets — never expose with a VITE_ prefix
DEEPSEEK_API_KEY=""
VOLC_APP_ID=""
VOLC_ACCESS_TOKEN=""
